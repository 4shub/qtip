export type ImportedSiteConfig = {
    headTags: string[];
    favicon: string;
};

export type SiteConfig = {
    headTags: string[];
    favicon: string;
    logo: {
        text: string;
        link: string;
    };
};
