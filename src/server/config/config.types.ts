export type ImportedSiteConfig = {
    headTags: string[];
    favicon: string;
    logo: {
        text: string;
        link: string;
    };
};

export type SiteConfig = {
    headTags: string[];
    favicon: string;
    logo: {
        text: string;
        link: string;
    };
};
